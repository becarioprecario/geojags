model{
  y[1:N] ~ dsar(mu[], W, rho, tau)
  for(i in 1:N) {
    mu[i] <- alpha + beta * x1[i]
  }

  tau <- 1/(sigma * sigma)

  alpha ~ dunif(-1000, 1000)
  beta ~ dnorm(0, 0.001)
  rho ~ dunif(-1, 1)
  sigma ~ dunif(0, 10)
}
