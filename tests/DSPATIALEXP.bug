model{
  y[1:N] ~ spatial.exp(mu[1:N], xx[1:N], yy[1:N], tau, phi, kappa)
  for(i in 1:N) {
    mu[i] <- alpha + beta * x1[i]
  }

  tau <- 1/(sigma * sigma)

  alpha ~ dunif(-1000, 1000)
  beta ~ dnorm(0, 0.001)
  
  phi ~ dunif(0, 2)
  kappa ~ dunif(0, 2)
  sigma ~ dunif(0, 10)
}
